services:
  - type: web
    name: bili-subtitles
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bili_subtitles.web_app:app --workers 2 --timeout 120 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11